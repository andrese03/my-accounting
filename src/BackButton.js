import React, { Component } from 'react';

class BackButton extends Component {
        render() {
            return <button className="btn btn-default">Back</button>
        }
}

export default BackButton;
