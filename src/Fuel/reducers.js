import {createRecord, updateRecord, deleteRecord } from './Fuel/actions.js'

function fuelRecords(state, action) {
    
    switch (action.type) {
        case 'FUEL_CREATE_RECORD':
            break;
        default:
            return state;
    }

}